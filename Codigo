print(" Avance de Proyecto 02 ")
print(" Registro y visualización de notas, cálculo de promedio ")

cantidad = int(input(" Ingrese la cantidad de Notas: "))

numeros = []

for i in range(cantidad):
    num = int(input(f"Ingresa la Nota {i+1}: "))
    print(" Se ha Registrado la Nota")
    numeros.append(num)

print("\nLas Notas Registradas son:", numeros)

Indicacion = input("¿Deseas Calcular el Promedio? (si/no): ")

if Indicacion.lower() == "si":
    print(" Obteniendo Tu Promedio...")
    prom = sum(numeros) / cantidad
    print(f"El promedio de las notas es: {prom:.1f}") 
elif Indicacion.lower() == "no":
    print(" Sin Promedio disponible.")
else:
    print(" Por favor escribe 'si' o 'no'.")

print(" Final de Registro y visualización de notas, cálculo de promedio ")



